using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteractableFoliageScript : MonoBehaviour
{

    /// <summary>
    /// NOTE: Attach this script to ANYTHING that you do not want rendered if the camera is not on it (Hint: Interactable foliage)
    /// </summary>
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
